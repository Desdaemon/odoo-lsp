<odoo>

	<record id="action_server_code" model="ir.actions.server">
		<field name="name">Server Action</field>
		<field name="model_id" ref="foo.model_foo"/>
		<field name="state">code</field>
		<field name="code">
			action = records.action_button()
			<!--             ^def -->
		</field>
	</record>

	<record id="action_server_env_ref" model="ir.actions.server">
		<field name="name">Server Action Env Ref</field>
		<field name="model_id" ref="foo.model_foo"/>
		<field name="state">code</field>
		<field name="code">
			action = env.ref("foo.action_server_code").sudo().read()[0]
			<!--                  ^def -->
		</field>
	</record>

	<record id="action_server_env_model" model="ir.actions.server">
		<field name="name">Server Action Env Model</field>
		<field name="model_id" ref="model_foo"/>
		<field name="state">code</field>
		<field name="code">
			action = env['clean.log.wizard'].display_wizard(records)
			<!--            ^def -->
			action = env['clean.log.wizard'].display_wizard(records)
			<!--                                 ^def -->
		</field>
	</record>

</odoo>
